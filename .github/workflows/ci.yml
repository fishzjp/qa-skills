name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    name: 架构红线与语法校验
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Skills 架构红线校验（frontmatter / ≤500 行 / When NOT to Use / 引用完整性 / JSON）
        run: python3 scripts/validate_skills.py

      # 评测 harness 的单测与语法检查随 eval/ 一并留本地（2026-08-22 起公开仓库只含 skills 产品内容）

      - uses: actions/setup-node@v7
        with:
          node-version: "22"

      - name: Node 语法检查（脚手架示例）
        run: |
          node --check examples/e2e-scaffold/tests/helpers.ts 2>/dev/null || echo "示例脚手架不在校验范围，跳过"

  label:
    name: PR 自动打标
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/labeler@v5
        with:
          configuration-path: .github/labeler.yml
