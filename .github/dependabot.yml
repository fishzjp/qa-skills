version: 2
updates:
  # npm 插件包（dsh-qa-skills）声明零运行时依赖，无可更新项——不配置 npm 生态
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    labels:
      - dependencies
