version: 2
updates:
  # npm 生态随 eval/harness 本地维护，公开仓库无 package.json——不配置
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    labels:
      - dependencies
