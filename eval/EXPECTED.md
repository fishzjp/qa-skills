# 预期效果门（Gates v1.0 冻结版）

> ## 验证轮判定（2026-08-20，run 20260820_1429_v2，冻结门后的独立新采样）
>
> **结果：4/7 通过（G2/G3/G5/G6 ✅），3 失败（G1a/G1b/G4 ❌）——按预注册纪律如实记录，不因数据改门。**
>
> | 门 | 结果 | 诊断 |
> |----|------|------|
> | G1a tcw 覆盖 Δ3.8pp（需 5pp） | ❌ | 方向显著（CI[0.5,7.1] 不含 0）但低于阈值。**阈值本身源自噪声膨胀的校准估计**（校准轮单采样 +28.8pp 的状态机差距在本轮多样本下缩至 +6.2pp——Off 基线采样 SD 达 14pp，校准轮抽到了弱基线） |
> | G1b 状态机 Δ6.2pp（需 20pp） | ❌ | 同上，20pp 阈值被证明不可达（基线饱和：Off 在该任务均值 87.3%） |
> | G4 api 编译 67% | ❌ | **真实失败，已修复**：1/3 采样输出超 32K token 上限被截断（285 行 `{` 未闭合）。修复 = api-testing SKILL.md 增加"输出预算纪律"（参数化收敛，防空截断），待下一验证轮检验 |
>
> **本轮可信结论（多样本 + 统计推断）**：Skill 的可复现效应是——可执行性 0.77→0.98（客观指标，无 judge 参与）、质量 +5.7pp（CI 显著）、植入 bug 检出 100%、E2E 真实执行 0%→78%（客观，Off 无一通过）、整体 11/12 不劣化。覆盖类增益 +3.8~5.2pp（方向显著，幅度小于校准轮表观值）。**成本：tokens 3.3×（26.3K vs 7.9K/任务）、用例数 3.8×**——Skill 是"更贵但显著更好"，Efficiency 数字如实披露。
>
> **遗留测量偏差（待 opencode 额度恢复复测）**：本轮 judge 为与生成同源模型（opencode 月度额度耗尽被迫降级），同源 judge 成对评审 20 对中 18 对判平、且 Off 覆盖打分可能偏高（宽容偏差）——覆盖/成对指标在异构 judge（kimi-k3）下预计更有区分度。恢复后一条命令重评：`EVAL_JUDGE_MODEL=oc:deepseek-v4-flash EVAL_PAIR_JUDGE_MODEL=oc:kimi-k3 python3 eval/harness/run_eval.py score --samples 3 --run-dir <run>`（judge/pairwise 缓存需先删）。泛化门 G7 同因额度阻塞未跑：`generate --samples 2 --model oc:deepseek-v4-pro`。
>
> ### v1.1 门提案（下一验证轮生效，以本轮多样本估计 + 边际导出；本轮判定不使用）
>
> | 门 | v1.0 | v1.1 提案 | 依据 |
> |----|------|-----------|------|
> | G1a | ≥5pp | ≥2pp 且 CI 下界 > 0 | 本轮 3.8pp[0.5,7.1]，取下界余量 |
> | G1b | ≥20pp | ≥3pp 且 CI 下界 > 0 | 本轮 6.2pp；基线饱和后状态机优势为温和正向 |
> | G4 | 100% | 保持 100%（已加输出预算纪律修复） | 截断为可修复的生成缺陷 |
> | 新 G-X1 执行成功 | 观察型 | E2E 执行通过率 On ≥ 60% 且 Off 差 ≥ 30pp | 本轮 On 78% vs Off 0% |

> **预注册声明**：本版门（G1–G7）于 **2026-08-20 冻结**，阈值由**校准轮**（runs/20260820_1124_v2，单采样旧 harness）数据加安全边际导出；随后的**验证轮**（多样本新 harness）为冻结后的独立新采样数据，其结果用于判定门通过与否——不再因验证轮数据调整阈值。新指标（执行成功率、成对胜率）本轮为观察型，积累两轮数据后于 v1.1 转为门。
>
> 科学性对应（对齐行业主流）：多样本生成（n=3）量化生成方差；任务层配对 bootstrap 95%CI（pass@k / τ-bench 式重复采样思想）；成对评审 + 位置互换（MT-Bench/AlpacaEval 范式，消除绝对打分刻度噪声与位置偏差）；LLM judge 3 采样多数表决；judge 与生成模型异构（glm-5.2 生成 / deepseek-v4-flash 评审 / kimi-k3 成对评审与标注审计）；真实执行验证（SWE-bench 式：E2E 跑真实浏览器 + mock 被测应用，API 跑真实 HTTP 服务 + pytest）；GT 标注由独立审计角色复核。

## 冻结的门（v1.0）

| 门 | 判定（冻结） | 阈值来源（校准轮实测 → 阈值） |
|----|--------------|------------------------------|
| G1 tcw 覆盖均值 | tcw 类有效覆盖均值差 ≥ **+5pp** 且 bootstrap 95%CI 下界 > 0 | 校准轮 Δ+6.8pp（无 CI）→ 取 5pp 底线并新增显著性要求 |
| G1b 状态机覆盖 | tcw-order-state 差 ≥ **+20pp** | 校准轮 Δ+28.8pp → 留 8.8pp 余量 |
| G2 可执行性 | tcw 类 On 可执行性均值 ≥ **0.85** | 校准轮 0.99 → 留 0.14 余量 |
| G3 Bug 检出 | tcw-user-delete-code On 检出 ≥ **75%**（4 个植入 bug ≥ 3） | 校准轮 75% → 保持底线（3/4 是下限语义） |
| G4 编译通过 | api + e2e 任务 On 编译通过率 **100%**（n=3 采样全过） | 校准轮 100% → 保持 |
| G5 质量 | 质量均分 On ≥ **0.80** 且 > Off | 校准轮 On 0.90 / Δ+6.1pp → 底线 0.80 |
| G6 整体不劣 | On 在 ≥ **2/3** 可比任务上不劣于 Off（-10pp 容差） | 校准轮 11/12 → 保持 2/3 底线 |
| G7 泛化 | 次生成模型（deepseek-v4-pro）上 tcw 有效覆盖方向性 **On > Off** | 新增（方向性检验，无阈值；n=2 采样） |

## 观察型指标（本轮采集，不判门，v1.1 转门）

| 指标 | 定义 | v1.1 转门预案 |
|------|------|---------------|
| X1 执行成功率 | E2E：生成 spec 在 mock 应用上真实浏览器执行的用例通过率；API：生成 pytest 对 mock 服务的通过率 | 阈值待两轮数据定 |
| X2 成对胜率 | 异构强模型（kimi-k3）成对评审 + 位置互换，On 胜率（平局 0.5）与 Wilson 95%CI | 目标方向：下界 > 50% |
| X3 生成方差 | 各任务有效覆盖的采样 SD | 用于解释个位数 pp 差异的可信度 |
| X4 Efficiency | 平均用例数 / 重复用例率 / 平均 tokens | 监控 Skill 的 token 成本不失控 |

## 已知限制（v2 评估体系仍有的缺口，如实声明）

1. **人工校准未做**：judge 与人工标注的一致性抽检（行业标准 ~20% 抽样）仍未执行——GT 标注经独立模型审计复核，但无真人介入
2. **成对评审与 judge 仍为同厂模型池**（opencode 通道），虽与生成模型异构，但非完全独立第三方
3. **单次验证轮**：多样本（n=3）已量化方差，但"验证轮复验"（对同一冻结门跑第二轮独立验证）待下个迭代窗口
4. **评测前导的单轮约束**：抑制 skill 澄清交互形态，测得的是"单轮交付形态下的 Skill 效果"（对 On/Off 对称生效，不影响对比有效性，但影响绝对分数的外推）
5. 黄金集 12 任务为 Agent 标注 + 独立模型审计；Benchmark 级扩容（含人工标注分母）仍是后续工作
